/* Find the largest palindrome made from the product of two 3-digit numbers. */

pub fn process() -> String {

    String::from("")
}

fn get_max_palindrome() -> String {
    /*
        The naive implementation is to start with largest 3 digit numbers
        and decrement downwards until the first palindrome is found.
    */

    let mut product: u64;
    let mut num1: u64 = 999;
    let mut num2: u64 = 999;

    while num1 > 99 {
        while num2 > 99 {
            product = num1 * num2;
            
        }
    }
}
